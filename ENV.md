# Admin Web Environment Variables

Set these in Vercel (Project Settings → Environment Variables):

- `API_BASE_URL` = `https://api.mysuperhero.xyz`
- `NEXT_PUBLIC_API_BASE_URL` = `https://api.mysuperhero.xyz`
- `DEV_SHOW_OTP` = `true` (demo only; shows OTP on login)
